<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sbs.jhs.at.dao.ArticleDao">
	<select id="getForPrintArticles" resultType="Article">
		SELECT *
		FROM article
		WHERE displayStatus = 1
		ORDER BY id DESC
		LIMIT #{limitFrom}, ${itemsInAPage}   
	</select>
	
	<select id="getForPrintListArticlesCount" resultType="int">
		SELECT COUNT(*) AS cnt
		FROM article
		WHERE displayStatus = 1
	</select>
	
	
	
	<select id="getForPrintArticleById" resultType="Article">
		SELECT *
		FROM article
		WHERE id = #{id}
	</select>

	<insert id="add" parameterType="Map" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO article
		SET regDate = NOW(),
		updateDate = NOW(),
		displayStatus = 1,
		title = #{title},
		body = #{body}
	</insert>
	
	<update id="modify" parameterType="map">
		UPDATE article
		SET updateDate = NOW(),
		title = #{title},
		body = #{body}
		WHERE id = #{id}
	</update>
	
	<update id="softDelete" parameterType="map">
		UPDATE article
		SET delStatus = 1,
		delDate = NOW(),
		displayStatus = 0
		WHERE id = #{id}
	</update>
	
	<update id="hitUp" parameterType="int">
		UPDATE article
		SET hit = hit + 1
		WHERE id = #{id}
	</update>
	
	<update id="deleteModify" parameterType="int">
		UPDATE article
		SET delStatus =1,
		dleDate = NOW(),
		displayStatus = 1,
		WHERE id = #{id}
	</update>
	<select id="getForPageMoveBeforeArticle" resultType="int">
		SELECT id
		FROM article
		WHERE id <![CDATA[<]]> #{id}
		AND displayStatus = 1
		ORDER BY id DESC
		LIMIT 1
	   
	</select>
	
	<select id="getForPageMoveAfterArticle" resultType="int">
	
		SELECT id
		FROM article
		WHERE id <![CDATA[>]]> #{id}
		AND displayStatus = 1
		ORDER BY id ASC
		LIMIT 1
	   
	</select>
	
</mapper>