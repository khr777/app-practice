<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sbs.jhs.at.dao.ArticleDao">
	<select id="getForPrintArticles" resultType="Article">
		SELECT *
		FROM article
		WHERE displayStatus = 1
		ORDER BY id DESC
	</select>

	<select id="getForPrintArticleById" resultType="Article">
		SELECT *
		FROM article
		WHERE id = #{id}
	</select>

	<insert id="add" parameterType="Map" useGeneratedKeys="true" keyProperty="newId">
		INSERT INTO article
		SET regDate = NOW(),
		updateDate = NOW(),
		displayStatus = 1,
		title = #{title},
		body = #{body}
	</insert>
	
	<update id="modify" parameterType="map">
		UPDATE article
		SET updateDate = NOW(),
		title = #{title},
		body = #{body}
		WHERE id = #{id}
	</update>
	
	<delete id="delete" parameterType="map">
		DELETE FROM article
		WHERE id = #{id}
	</delete>
	
	<update id="hitUp" parameterType="int">
		UPDATE article
		SET hit = hit + 1
		WHERE id = #{id}
	</update>
	
	
</mapper>